/**
 * Due to monorepo structure, this cannot be referenced from src/enums/track-type.ts but should be
 * kept in sync
 */
enum TrackType {
    Sequencer = "sequencer",
    Instrument = "instrument",
}

export { TrackType };
